$ruby

puts "Welcome to this adventure.
puts "What is your name?"
name = gets.chomp
  if name.is_a? string
    puts "Hello, #{name}, are you ready to begin?"
    ready = gets.chomp
  else
    puts "Please enter your name."
    name = gets.chomp
  end
end
    
